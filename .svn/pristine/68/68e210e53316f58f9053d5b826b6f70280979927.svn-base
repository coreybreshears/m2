<%= select_tag @select_name || :tp_id,
               options_for_select(@tps.empty? ? [[]] : @tps.sort_by(&:nice_name).map { |tp| [tp.nice_name, tp.id] }),
               autocomplete: 'off',
               disabled: @tps.empty?
%>